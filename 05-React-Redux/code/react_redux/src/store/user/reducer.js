
const initialState = {
  userInfo: {
    name: 'tony',
    age: 18
  }
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer